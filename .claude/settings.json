{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *\"Agentisk Koding.html\") python3 \"$CLAUDE_PROJECT_DIR/.claude/hooks/check-deck-sync.py\" \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Checking deck slide/notes sync..."
          }
        ]
      }
    ]
  }
}
